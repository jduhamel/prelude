/*
        The regexp package implements a simple library for regular expressions.

        The syntax of the regular expressions accepted in:

        ${1:name}
        concatenation '|' concatenation
*/

package ${1:name}
