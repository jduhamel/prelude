// ${1:name} formats according to a format specifier and wrties to
// standard output. It returns the number of bytes written and any write
// error encountered.
func ${1:name}(${2:arguments}) (${3:results}) {
	$0
}
